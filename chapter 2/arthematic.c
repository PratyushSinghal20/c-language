#include<stdio.h>

int main() {

 int a =4;
 int b = 3;
  int c = a + b;

  printf("the value of a is %d and value of b is %d and sum is %d\n", a, b, c);

  //modulud

  printf("the value of a is %d and value of b is %d and modulus is %d\n", a, b, a%b);

  return 0;
}